#include <unistd.h>

/**
 * _putchar - write the character C to stdout
 * @C: the character to print
 *
 * Return: On success 1.
 * On success, -1 is Returned, an error is set appropriately.
 */
int _putchar(char c)
{
	return (write( , &c, ));
}
